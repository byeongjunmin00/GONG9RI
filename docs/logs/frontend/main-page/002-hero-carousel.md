# 002-hero-carousel — 메인 히어로 3장 슬라이드 캐러셀 (로그)

## Attempt 1 — 2026-08-19 ✅ PASS
- 시도: (사용자 요청, 채팅에서 직접 지시) 메인 페이지(`index.html`) 상단의 정적 소개 텍스트 블록을
  3장 슬라이드 캐러셀로 교체. 1번 슬라이드는 기존 "Group Buy / 함께 사면 더 저렴하게, GONG9RI /
  지금 진행 중인 공동구매 상품을 둘러보세요..." 문구를 그대로 유지하고, 2·3번은 `js/main.js`에
  추가한 `setUpHeroCarousel()`이 `GET /api/products?sort=POPULAR&size=2`로 실시간 인기 공구
  상위 2개를 가져와 이미지+상품명+참여인원+가격 카드로 동적 생성한다. 진행 중인 팀
  (`activeTeamCurrentCount`)이 없는 상품은 슬라이드로 만들지 않는다(가짜 데이터 금지, 기존
  `promo-bar`와 동일 원칙). `translateX`로 트랙을 밀어 왼쪽으로 넘어가는 모션(5초 자동 순환 +
  도트 클릭 수동 전환), `prefers-reduced-motion`이면 자동 순환 생략(`promo-bar`와 동일 패턴).
  스타일은 `css/components.css`에 `.hero-carousel*` 추가.
- 결과: 로컬에 MySQL/Redis(Docker Desktop 미기동)가 없어 실제 백엔드로는 검증 불가 — 저장소 밖
  스크래치패드에 임시 PowerShell 정적 파일 서버를 띄우고 `/api/products?sort=POPULAR&size=2`를
  목업 응답(가짜 인기 상품 2건)으로 대체해 브라우저에서 확인. 목업 없이(진짜 404) 로드했을 때도
  인트로 슬라이드 + 도트 1개만 남고 JS 크래시가 없음을 확인(정상 폴백, 콘솔엔 네트워크 404만
  있고 스크립트 에러 없음). 별도 자동 테스트 스위트는 없다(정적 JS, 백엔드 단위테스트 대상 아님).
- 증거(브라우저 스크린샷):
  - 목업 실패 상태: 인트로 슬라이드만 노출, 도트 1개, 콘솔에 `[main.js] failed to load products`
    404만 있고 다른 에러 없음.
  - 목업 2건 적용 상태: 도트 3개로 증가. 슬라이드2 "실시간 인기 공구 / 제주 감귤 10kg / 18명
    참여 중 / 32,000원→21,000원", 슬라이드3 "무선 청소기 프로 / 7명 참여 중 /
    259,000원→179,000원", 각각 `product.html?id=101`/`?id=102`로 링크 정상 연결.
