# 003-merge-carousels — 히어로 캐러셀 + 공지 바 통합, 풀블리드 이미지 테마 (로그)

## Attempt 1 — 2026-08-19  ✅ PASS
- 시도: (사용자 리포트) 002에서 만든 소개 캐러셀 바로 위에 기존 "슬림 공지 바"(`#promo-bar`,
  `setUpPromoBar()`)가 남아 있어 두 슬라이드 UI가 겹쳐 보여 난잡하다는 지적. 두 컴포넌트를 하나로
  합쳤다 — 크기(고정 높이 300px 배너)는 기존 소개 캐러셀, 테마(어두운 배경 + 흰 글씨)는 기존
  공지 바를 따랐다. `index.html`의 `#promo-bar`와 기존 `#hero-carousel` 마크업을 하나의
  `#hero-carousel`로 교체(1.소개 2.실시간 인기 공구 3.신규 가입 — 사용자가 추가 요청으로 "모일수록
  저렴해지는/지금 진행 중인 공구팀 둘러보기" 4번째 슬라이드는 제외). `js/main.js`의
  `setUpPromoBar()`/`setUpHeroCarousel()` 두 IIFE를 하나의 `setUpHeroCarousel()`로 통합 — 1·3번은
  정적 마크업 그대로 두고, 2번(인기 공구)만 `GET /api/products?sort=POPULAR&size=1`로 실제 인기
  1위 상품이 있으면(`activeTeamCurrentCount`) 그 상품 이미지·이름·참여인원·가격으로 덮어쓴다(없으면
  기본 문구 유지, 가짜 데이터 금지 원칙 유지). `components.css`의 옛 `.hero-carousel-product-*`,
  `.hero-carousel-slide--intro/--product`, `.promo-bar*` 규칙을 전부 지우고 `.hero-carousel-slide`
  공통 스타일(배경 풀블리드, `::before` 하단 그라디언트 스크림, 흰 글씨 콘텐츠 오버레이)로 교체.
  사용자가 "카카오 프렌즈" 공식 캐릭터 이미지를 신규가입 슬라이드에 써달라고 요청했으나, Kakao Corp
  소유의 상표/저작권 이미지라 무단 사용 시 공식 제휴로 오인될 수 있어 반영하지 않고, 기존
  로그인 버튼과 동일하게 이모지+텍스트만 유지(브랜드 컬러 `#FEE500`는 로그인 버튼에서만 이미 사용 중).
- 결과: 로컬 MySQL/Redis(Docker Desktop 미기동)가 없어 실제 백엔드 대신 스크래치패드의 임시
  PowerShell 목업 서버(`/api/products?sort=POPULAR&size=1,2`)로 확인. 슬라이드 3개, 도트 3개로
  줄어든 것 확인. 2번 슬라이드가 목업 인기 상품("제주 감귤 10kg") 실제 이미지를 배경으로 꽉 채우고
  하단에 흰 글씨로 "🔥 실시간 인기 공구 / 제주 감귤 10kg / 18명 참여 중 · 32,000원 → 21,000원" 노출,
  `product.html?id=101`로 링크 정상. 1번(소개)·3번(카카오 가입)은 브랜드 그라디언트 배경 + 흰 글씨로
  정상 노출. 별도 자동 테스트 스위트는 없다(정적 JS).
- 증거(브라우저 스크린샷):
  - 슬라이드1: 브랜드 그라디언트 배경, "GROUP BUY / 함께 사면 더 저렴하게, GONG9RI / 지금 진행
    중인..." 흰 글씨.
  - 슬라이드2: 목업 상품 사진이 배경 전체를 채움, "🔥 실시간 인기 공구 / 제주 감귤 10kg / 18명
    참여 중 · 32,000원 → 21,000원" 흰 글씨, `product.html?id=101` 링크.
  - 슬라이드3: 브랜드 그라디언트 배경, "🎉 신규 가입하면 / 카카오톡으로 3초만에 시작" 흰 글씨,
    `/api/auth/kakao/login` 링크.
  - 도트 3개로 감소, 자동 순환 및 클릭 전환 정상.
