# 002-ai-suggest-rate-limit — 상품등록 도우미 트래픽 제어 (로그)

## Attempt 1 — 2026-08-20  ✅ PASS
- 시도: `POST /api/seller/products/ai-suggest`에 `RateLimitRule` 추가(1분/5회). OpenAI를 호출하는
  엔드포인트가 둘인데 챗봇에만 규칙이 있고 이쪽은 빠져 있던 갭을 메움 — 챗봇 규칙을 추가하던 08-19에
  이미 발견해놓고 그때 처리하지 않아 하루 넘게 한쪽만 막힌 상태로 운영됐다.
- 결과: `./gradlew test` 전체 **401케이스 통과**.
- 증거(API 샘플, 프로덕션 실측): 같은 IP로 7회 연속 호출 —
  ```
  1~5번째: 401 (필터 통과 후 인증 단계에서 거절)
  6번째:   429 TOO_MANY_REQUESTS
  7번째:   429
  ```
  설계값(1분/5회)대로 정확히 6번째부터 차단됨을 확인.
- 테스트: `AiSuggestRateLimitFilterTest` 3케이스(임계값 이내 통과 / 초과 429 / **접두사가 같은 이웃
  엔드포인트 `POST /api/seller/products`는 대상 아님** — 정규식 과잉 매칭 방지). **규칙을 제거하면 실제로
  실패하는 것까지 역검증함.**
- 특기: 챗봇(1분 10회)보다 낮게 잡은 근거 — 상품 하나 등록 전 초안을 받는 용도라 연속 호출이 드물고,
  호출당 프롬프트에 카테고리 템플릿 전문이 들어가 단가가 높다.
