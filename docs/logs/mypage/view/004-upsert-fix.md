# 004-upsert-fix — 판매자 수익 요약(seller_revenue_summary) 조회 시 부트스트랩 → 결제 시 upsert 정정 (로그)

## Attempt 1 — 2026-08-05

- 시도: `docs/dev/ongoing/seller-revenue-summary-upsert-fix.md` 승인된 계획대로, 003(`docs/dev/mypage/view/changes/003-precomputed-revenue.md`)에서 남겨뒀던 "부트스트랩 vs 신규 결제 경쟁" 잔여 리스크를 근본적으로 없앴다 — 요약 행 생성 시점을 "판매자가 조회할 때(지연 부트스트랩)"에서 "결제가 발생할 때(upsert)"로 옮겼다.
  - **`SellerRevenueSummaryRepository.incrementPaid`를 upsert로 전환**: 기존 JPQL 조건부 UPDATE(`UPDATE ... WHERE seller.id = :sellerId`, 행 없으면 0 rows affected로 조용히 무시)를 네이티브 SQL `INSERT INTO seller_revenue_summary (...) VALUES (:sellerId, :amount, 1, 0, ...) ON DUPLICATE KEY UPDATE total_revenue = total_revenue + :amount, paid_count = paid_count + 1, ...`로 교체했다(`@Query(nativeQuery = true)` + `@Modifying(flushAutomatically = true, clearAutomatically = true)`). MySQL이 `UNIQUE(seller_id)` 충돌 시 그 행에 락을 걸고 UPDATE로 전환하는 단일 SQL 문이라는 점을 근거로 삼았다 — 애플리케이션 레벨에서 "조회 후 있으면 update, 없으면 insert" 두 단계로 구현하지 않고 DB에 원자성을 위임했다. 반환형은 더 이상 의미 있는 rows-affected 정보가 아니라서(항상 성공) `int`에서 `void`로 바꿨다(호출부도 이미 반환값을 안 쓰고 있었음을 확인).
  - **`applyRefund` 재검토**: 로직 자체는 그대로(조건부 UPDATE, 환불된 결제 금액합/건수를 한 번에 반영) 두되, 전제(결제 시점에 이미 요약 행이 있어야 함)와 그 전제가 깨지는 유일한 경우(upsert 전환 이전부터 있던 결제 이력이 아직 백필 안 된 판매자에게 환불이 먼저 들어오는 것)를 리포지토리 주석으로 명시했다. `TeamDeadlineService.processDeadline()`이 `applyRefund`의 리턴값(rows affected)을 받아 0이면 `log.warn(...)`으로 sellerId/teamId/refundedAmount/refundedCount를 남기도록 고쳤다 — `docs/code-convention.md`의 로깅 레벨 기준("WARN = 복구 가능한 이상 상황")에 맞춰, 조용히 무시되던 걸 드러나게 했다. 완벽히 해결하라는 요구가 아니라 리스크 문서화가 목표였으므로 그 이상은 하지 않았다.
  - **`SellerMypageService.revenue()` 단순화**: `bootstrapRevenueSummary`(집계 쿼리로 실제 값을 계산해 insert, `DataIntegrityViolationException` 캐치해 경쟁 방어) 메서드를 통째로 제거했다. 이제 `sellerRevenueSummaryRepository.findBySellerId(sellerId).map(RevenueResponse::from).orElseGet(RevenueResponse::empty)`만 남았다 — 순수 읽기. 메서드 레벨 `@Transactional`(쓰기 허용용으로 readOnly를 오버라이드하던 것)도 제거해 클래스 기본 `@Transactional(readOnly = true)`로 되돌렸다. 더 이상 안 쓰는 `PaymentRepository`/`MemberRepository` 필드·`RevenueSummaryProjection`/`DataIntegrityViolationException`/`SellerRevenueSummary`/`Member` import를 정리했다(이 서비스에서 그 두 리포지토리는 오직 부트스트랩에만 쓰였다는 걸 grep으로 재확인한 뒤 제거).
  - **`RevenueResponse.empty()` 신규**: 요약 행이 없을 때(결제 이력이 아예 없는 판매자) 그냥 `new RevenueResponse(0, 0L, 0L)`을 돌려주는 정적 팩토리를 추가했다.
  - **1회성 백필 메커니즘**: "조회마다 실행되면 안 된다"(그러면 지금 없애는 지연 부트스트랩과 같은 문제가 재발)는 제약을 지키기 위해, 배포 시 opt-in으로만 켜지는 별도 컴포넌트를 새로 만들었다.
    - `PaymentRepository.findDistinctSellerIdsWithPayments()`(신규): `SELECT DISTINCT p.product.seller.id FROM Payment p` — 백필 후보(결제 이력이 있는 판매자 전체) 탐색용.
    - `service/SellerRevenueSummaryBackfillService.java`(신규): `backfillMissingSummaries()`가 위 후보를 순회하며 `backfillOneIfMissing(sellerId)`(판매자 1명 단위 `@Transactional`, `TeamDeadlineService`가 팀 단위로 트랜잭션을 쪼개는 것과 같은 이유 — 전체를 한 트랜잭션으로 묶지 않음)를 호출한다. 이미 요약 행이 있으면 건드리지 않고 스킵, 없으면 기존 집계 쿼리(`findRevenueSummaryBySellerId`)로 재계산해 `save()` — 동시에 그 판매자의 "첫 결제"가 막 upsert로 행을 만든 경우와 경합할 수 있어 `DataIntegrityViolationException`을 캐치해 조용히 스킵하도록 했다(003의 `bootstrapRevenueSummary`와 같은 방어 패턴 재사용, 단 이번엔 배치 컨텍스트).
    - `batch/SellerRevenueSummaryBackfillRunner.java`(신규 패키지, `scheduler/`와 같은 위치의 형제 패키지): `ApplicationRunner` + `@ConditionalOnProperty(name = "app.backfill.seller-revenue-summary", havingValue = "true")` — `application.yaml`에 아무 설정이 없으면 이 빈 자체가 등록되지 않아 기본적으로 완전히 비활성이다(조회마다 실행되는 지연 부트스트랩과는 근본적으로 다른 트리거 방식). `application.yaml`에 새 설정을 추가하지 않았다 — property가 아예 없으면 `@ConditionalOnProperty`가 매치 안 되는 게 정확히 원하는 "기본 꺼짐"이라, 배포 시 환경변수/커맨드라인 인자로만 켜는 절차를 문서(`docs/db/seller_revenue_summary.md`)에 남겼다. 현재 로컬 DB는 결제 0건이라 실제로 이 러너를 실행해보진 않았다(계획 문서에서도 "실행 가능한 메커니즘만 마련, 실제 실행은 불필요"라고 명시).
    - `PaymentRepository.findRevenueSummaryBySellerId`는 계획대로 삭제하지 않고 유지(백필 + 드리프트 검증 양쪽에서 계속 씀).
  - **테스트**:
    - `SellerRevenueSummaryTest.java`: 옛 `revenue_bootstrapsFromExistingPaymentsWhenSummaryRowMissing`(조회 시 부트스트랩 검증)을 `backfill_fillsSummaryFromExistingPaymentsWhenRowMissing`로 교체 — 같은 시나리오(과거 결제 이력만 있고 요약 행 없음)를 재현하지만, 이제 `sellerMypageService.revenue()`는 순수 읽기(0을 반환하고 행을 안 만듦)임을 먼저 확인한 뒤 `sellerRevenueSummaryBackfillService.backfillOneIfMissing(sellerId)`를 직접 호출해 채워지는지, 그리고 재실행 시 멱등(이미 있으면 다시 안 만듦)한지까지 검증한다. `paymentCreate_incrementsSummaryExactly`/`teamDeadlineRefund_decrementsSummaryExactly`/`summaryMatchesRecomputedAggregate_forBulkDummyData`에서 사전에 `sellerMypageService.revenue()`를 호출해 0행을 부트스트랩해두던 준비 코드를 전부 제거했다(이제 첫 결제가 upsert로 스스로 행을 만드므로 불필요). 결제 이력이 아예 없는 판매자가 순수 읽기로 정확히 0을 반환하는 걸 확인하는 `revenue_returnsZeroWhenNoPaymentsEverHappened`를 신규로 추가했다.
    - `SellerRevenueSummaryConcurrencyTest.java`: 기존엔 스레드를 시작하기 전에 `sellerMypageService.revenue()`로 요약 행을 0으로 미리 만들어뒀는데(당시엔 "조건부 UPDATE라 행이 미리 있어야 반영된다"는 이유), 이번 계획이 정면으로 검증하라고 요구한 시나리오("요약 행이 아예 없는 상태에서 최초 동시 결제")를 만들기 위해 그 사전 준비 줄을 지웠다 — `initial` 조회는 남겨서 "조회만으로는 행이 생기지 않는다"를 같이 확인하게 했다. 20개 스레드가 동시에 같은 판매자에게 "첫 결제"를 쏘는 상태에서 `summaryIdToClean`을 스레드 실행 **후**(요약 행이 그 경쟁 중 생성된 뒤) 캡처하도록 옮겼다. 최종 합계가 `basePrice * threadCount`와 정확히 일치하는지, 원본 payment 재계산 값과 드리프트 없는지를 그대로 검증한다.
    - `SellerMypageControllerTest.revenue_success_excludesRefundedFromTotal`: 이 테스트가 `paymentRepository.save(new Payment(...))`로 결제를 직접 꽂아 넣고(`PaymentService.create`/`incrementPaid`를 거치지 않음) 옛 지연 부트스트랩에 의존해 통과하던 걸 발견했다 — 이번 변경으로 `revenue()`가 순수 읽기가 되면서 이 테스트가 그대로면 깨진다(요약 행이 전혀 없어 0을 반환하게 됨). 컨트롤러 테스트의 실제 관심사(HTTP 계층이 요약값을 정확히 그대로 응답하는지, PAID/REFUNDED 집계 수식 자체는 서비스 레벨에서 이미 검증됨)에 맞게, `sellerRevenueSummaryRepository.save(new SellerRevenueSummary(seller, 40000, 2L, 1L))`로 요약 행을 직접 seed하는 방식으로 고쳤다. 더 이상 안 쓰는 `PaymentRepository`/`Payment` import·필드를 제거하고 `SellerRevenueSummaryRepository`/`SellerRevenueSummary`를 추가했다.
  - **문서**: `docs/db/seller_revenue_summary.md`(컬럼 집계 방식 섹션 전체를 upsert 전환 서술로 교체 — 요약 행 생성 시점 변경, applyRefund 전제/WARN, 백필 메커니즘·opt-in 실행 절차 포함), `docs/dev/mypage/view/design.md`("판매자 수익 집계" 불릿을 새 방식으로 교체, "관련 코드 위치"에 신규 파일(`SellerRevenueSummaryBackfillService`, `batch/SellerRevenueSummaryBackfillRunner`, `findDistinctSellerIdsWithPayments`, `RevenueResponse.empty()`) 추가, 테스트 목록 갱신). `docs/dev/ongoing/seller-revenue-summary-upsert-fix.md`는 계획대로 건드리지 않았다(이동은 Evaluate 몫).
- `./gradlew compileJava compileTestJava` → `BUILD SUCCESSFUL`.
- `./gradlew test --tests "*SellerRevenueSummary*" --tests "*SellerMypage*"`(로컬 Docker `gong9ri-mysql`/`gong9ri-redis` 가동 상태) → `BUILD SUCCESSFUL`. `build/test-results/test/*.xml` 확인: `SellerMypageControllerTest`(11건), `SellerRevenueSummaryTest`(5건, 옛 4건 + 신규 zero/backfill 테스트 반영), `SellerRevenueSummaryConcurrencyTest`(1건) 전부 `failures="0" errors="0"`.
- `SellerRevenueSummaryConcurrencyTest`를 `--rerun`으로 3회 연속 재실행 → 3회 모두 `BUILD SUCCESSFUL`(요약 행이 아예 없는 상태에서 시작하는 새 경쟁 시나리오로도 플래키니스 없음).
- `./gradlew build`(전체 스위트) → `BUILD SUCCESSFUL`. `build/test-results/test/*.xml` 14개 클래스 합산 `tests=85, failures=0, errors=0`(003 완료 시점 84건 + 이번에 net 1건 증가: 부트스트랩 테스트를 백필 테스트로 교체(±0) + zero-revenue 테스트 신규(+1)).

## Evaluate — 2026-08-06  ✅ PASS

- **계산적 평가**
  - Docker `gong9ri-mysql`(3306)/`gong9ri-redis`(6379) 가동 확인 후 `./gradlew clean build` 직접 재실행 → `BUILD SUCCESSFUL`. `build/test-results/test/*.xml` 14개 클래스 합산 `tests=85, failures=0, errors=0`(generator 보고와 일치).
  - `SellerRevenueSummaryConcurrencyTest`를 `--rerun`으로 **5회 연속** 재실행 → 5회 모두 `BUILD SUCCESSFUL`(exit 0). 마지막 실행의 XML(`TEST-...SellerRevenueSummaryConcurrencyTest.xml`)을 직접 열어 `tests="1" failures="0" errors="0"`과 실제 Hibernate SQL 로그를 확인 — `INSERT INTO seller_revenue_summary (...) VALUES (?, ?, 1, 0, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP) ON DUPLICATE KEY UPDATE total_revenue = total_revenue + ?, paid_count = paid_count + 1, ...`가 20개 스레드의 "첫 결제" 각각에서 그대로 실행되는 걸 확인함(캐시된 결과가 아니라 매번 실제로 컨테이너 대상 MySQL에 붙어 실행됨). 새 시나리오(요약 행이 아예 없는 상태에서 시작하는 경쟁)로도 플래키니스 관찰되지 않음.
  - 죽은 코드 정리(`RevenueResponse.from(RevenueSummaryProjection)` 제거) 후 재빌드까지 재확인 → 여전히 `BUILD SUCCESSFUL`, `tests=85, failures=0, errors=0`(제거된 오버로드를 쓰는 테스트가 없었으므로 회귀 없음).

- **추론적 평가**
  - `SellerRevenueSummaryRepository.incrementPaid`: 네이티브 `INSERT ... ON DUPLICATE KEY UPDATE` 단일 SQL 문으로 직접 확인함(`@Modifying(flushAutomatically = true, clearAutomatically = true)` + `nativeQuery = true`). MySQL/InnoDB가 `UNIQUE(seller_id)` 충돌 시 해당 행에 락을 걸고 UPDATE로 전환하는 알려진 동작을 코드가 올바르게 활용한 패턴(애플리케이션 레벨의 "조회 후 분기" 없이 원자성을 DB에 위임) — 코드 자체가 이 패턴을 정확히 구현했는지만 확인 대상이었고, 확인됨.
  - `SellerMypageService.revenue()`: 코드를 직접 읽어 `findBySellerId(...).map(RevenueResponse::from).orElseGet(RevenueResponse::empty)`만 있고 쓰기 로직(저장/증분)이 전혀 없음을 확인. 클래스 레벨 `@Transactional(readOnly = true)`만 있고 메서드 레벨 오버라이드가 없어 정확히 readOnly 트랜잭션으로 동작함.
  - 백필 비활성화: `application.yaml`에 `app.backfill.seller-revenue-summary` 프로퍼티가 없음을 grep으로 확인 — `@ConditionalOnProperty(havingValue = "true")`라 프로퍼티가 없으면 `SellerRevenueSummaryBackfillRunner` 빈 자체가 등록되지 않는다(기동/배포마다 자동 실행되지 않음). 계획의 "조회마다 실행되면 안 된다" 제약과 별개로 "기동마다 자동 실행되면 안 된다"도 만족.
  - `applyRefund` 0건 WARN: `TeamDeadlineService.processDeadline()` 코드에서 `rowsAffected == 0`일 때 `log.warn(...)`으로 sellerId/teamId/refundedAmount/refundedCount를 남기는 걸 확인. `docs/code-convention.md`의 "WARN = 복구 가능한 이상 상황" 기준에 부합(에러로 죽지 않지만 데이터 정합성 이상 신호를 눈에 보이게 남김) — "조용히 무시"에서 "적어도 드러나게"로 바뀌었다는 계획 의도와 일치.
  - **`RevenueResponse.from(RevenueSummaryProjection)` 죽은 코드**: `grep -rn "RevenueResponse.from(" src/`로 전체 재확인 — 정의부(`RevenueResponse.java`) 외 호출부가 프로덕션·테스트 어디에도 없음을 확인. 이번 변경(부트스트랩 제거)이 만든 부산물이라 판단해 이번 스코프에서 직접 제거(관련 `import RevenueSummaryProjection`도 함께 제거). 제거 후 재빌드로 회귀 없음 확인(`tests=85, failures=0, errors=0`).
  - 테스트 의미성: `SellerRevenueSummaryTest`(zero-revenue, 결제 시 upsert 증가, 환불 감소, 백필+멱등성)와 `SellerRevenueSummaryConcurrencyTest`(요약 행 없는 상태에서 20스레드 동시 첫 결제)를 코드로 직접 읽어 확인 — 형식적 assert가 아니라 "조회만으로는 행이 안 생긴다", "백필 재실행 시 기존 행을 안 건드린다(멱등)", "동시 경쟁 후 원본 payment 재계산 값과 드리프트 없음"처럼 이번 변경의 핵심 동작을 정면으로 검증함. `SellerMypageControllerTest.revenue_success_excludesRefundedFromTotal`도 `SellerRevenueSummary`를 직접 seed하는 방식으로 바뀌어, 옛 지연 부트스트랩 의존이 제거됐음을 확인.
  - 문서 대조: `docs/db/seller_revenue_summary.md`, `docs/dev/mypage/view/design.md`를 열어 새 방식(결제 시점 upsert, 조회는 순수 읽기, 백필은 opt-in 별도 실행, WARN 로그, 백필 이후 죽은 코드 정리)이 정확히 반영돼 있음을 확인.
  - 컨벤션: 생성자 주입(`@RequiredArgsConstructor` + `final` 필드), 계층 분리(레포지토리=쿼리, 서비스=트랜잭션 경계), `@Slf4j` 사용(println 없음), 로그에 도메인 식별자 포함 등 `docs/code-convention.md` 위반 없음.
  - `git status`/`git diff`로 004 스코프 확인: `docs/ERD.md`, `docs/policy/caching.md`, `CacheConfig.java`, `CacheConfigTest.java`, `SellerRevenueCachingTest.java` 삭제 등은 diff에 섞여 있으나 grep으로 확인한 결과 이 파일들엔 upsert-fix/004 관련 내용이 전혀 없음 — 미커밋 상태로 남아있던 003(집계 컬럼 전환, Redis 캐싱 제거)의 잔여 변경이며 004 Generate가 건드린 범위가 아님을 확인. `PaymentService.java`/`PaymentRepository.java`의 diff도 마찬가지로 캐시 무효화→컬럼 증가 전환(003)과 `findDistinctSellerIdsWithPayments` 신규(004)가 섞여 있었는데, 004에 해당하는 부분만 계획과 일치함을 확인. 004 자체의 범위 침범은 없음.

- **판정**: **PASS**. `docs/dev/ongoing/seller-revenue-summary-upsert-fix.md`를 `docs/dev/mypage/view/changes/004-upsert-fix.md`로 채번 이동하고, `docs/dev/mypage/view/design.md`를 최종 상태로 갱신함(죽은 코드 정리 내역 추가).
