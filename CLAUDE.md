# CLAUDE.md

이 저장소의 실제 작업 규칙은 **`AGENTS.md`**(저장소 루트)에 있다.

**작업을 시작하기 전에 반드시 `AGENTS.md`를 먼저 읽는다.** 실행 흐름(Plan → 휴먼게이트 → Generate → Evaluate), 프로젝트 개요, 명령어, 어떤 상황에 어떤 문서를 읽어야 하는지(컨텍스트 맵)가 전부 거기 정리돼 있다.

이 파일(`CLAUDE.md`)은 Claude Code가 세션 시작 시 자동으로 읽는 진입점 역할만 한다 — `AGENTS.md`는 Claude Code뿐 아니라 다른 도구(Codex 등)와도 공유하는 공통 규칙 파일이라 내용을 여기 중복해서 적지 않는다.
